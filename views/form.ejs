<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reservation Form</title>
  <style>
  .container {
        background-image: url("https://images.pexels.com/photos/70842/hot-pepper-pepper-fire-food-70842.jpeg");
        position: relative;
        width: 1320px;
        height: 690px;
        background-size: cover;
        background-color:black;
        background-position: center;
        display: flex;
        padding: 80px 100px 2px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }

    .container .box1 {
        max-width: 600px;
        color: black;
        margin-left:200px;
    }

    .box1 h2 {
        font-size: 2em;
        letter-spacing: .03em;
    }

    .box1 p {
        font-size: 1.2em;
        margin: 10px 0 40px;
    }

    .container .box2 {
        margin-left: 150px;
        position: relative;
        width: 500px;
    }

    .box2 h2 {
        font-size: 1.5em;
        color: #fff;
        text-align: center;
    }

    .box2 .input1 {
        position: relative;
        width: 100%;
        height: 50px;
        margin-bottom: 7px;
    }

    .input1 input[type="text"],
    .input1 input[type="email"],
    .input1 input[type="tel"],
    .input1 input[type="date"],
    .input1 input[type="time"],
    .input1 input[type="number"],
    .input1 textarea#special_requests {
        color: #fff;
        width: 100%;
        height: 50%;
        background: transparent;
        border: 1px solid #fff;
        outline: none;
        border-radius: 40px;
    }

    .input1 label {
        top: 50%;
        left: 45px;
        transform: translate(-50%);
        font-size: 17px;
        color: #fff;
        pointer-events: none;
    }

    #date,
    #time {
        color: #fff;
    }

    input[type="date"]::-webkit-calendar-picker-indicator,
    input[type="time"]::-webkit-calendar-picker-indicator {
        background-color: #fff;
        margin-right: 1px;
        padding: 5px;
        cursor: pointer;
        border-radius: 20px;
    }

    .input1 #btn {
        width: 100%;
        height: 30px;
        border: none;
        outline: none;
        border-radius: 40px;
    }

    .box {
        border-radius: 10px;
        overflow: hidden;
    }

    .box video {
        width: 100%;
        height: auto;
    }

        
    h2 a {
        text-align: center;
        margin-bottom: 30px;
        color: white;
        text-decoration: none;
    }
    label{
        color: black;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="box2">
      <h2></h2>
      <form action="https://api.web3forms.com/submit" method="POST" id="form1">
            <input type="hidden" name="access_key" value="810e3275-ca64-40d9-8244-0c70a99bf626">
      <form method="post" action="/reserve" id="form">
        <div class="input1">
          <label for="name">Name:</label>
          <input type="text" id="name" name="name" required>
        </div>
        <div class="input1">
          <label for="email">Email:</label>
          <input type="email" id="email" name="email" required>
        </div>
        <div class="input1">
          <label for="phone">Phone:</label>
          <input type="tel" id="phone" name="phone" required>
        </div>
        <div class="input1">
          <label for="location">Location:</label>
          <input type="text" id="location" name="location" required>
        </div>
        <div class="input1">
          <label for="date">Date:</label>
          <input type="date" id="date" name="date" required>
        </div>
        <div class="input1">
          <label for="time">Time:</label>
          <input type="time" id="time" name="time" required>
        </div>
        <div class="input1">
          <label for="people">Number of People:</label>
          <input type="number" id="people" name="people" required>
        </div>
        <div class="input1" style="grid-column: span 2;">
          <label for="special_requests">Special Requests:</label>
          <textarea id="special_requests" name="special_requests" rows="4"></textarea>
        </div>
        <div class="input1" style="grid-column: span 2;">
          <input type="submit" id="btn" value="Book now" onclick="openPopup()">
        </div>
      </form>
      </form>
    </div>
        <div class="container1">
      <div class="box1">
        <h2 >
      <a href="" class="typewrite" data-period="2000"
        data-type='[ "FEELING HUNGRY?", "PLAN A DATE", "WITH FRIENDS AND FAMILY", "BOOK NOW..." ]'>
        <span class="wrap"></span>
      </a>
    </h2>
        
      <div class="box">
       
      </div>
    </div>
  </div>
  <script>
    var TxtType = function(el, toRotate, period) {
        this.toRotate = toRotate;
        this.el = el;
        this.loopNum = 0;
        this.period = parseInt(period, 10) || 2000;
        this.txt = '';
        this.tick();
        this.isDeleting = false;
    };

    TxtType.prototype.tick = function() {
        var i = this.loopNum % this.toRotate.length;
        var fullTxt = this.toRotate[i];

        if (this.isDeleting) {
        this.txt = fullTxt.substring(0, this.txt.length - 1);
        } else {
        this.txt = fullTxt.substring(0, this.txt.length + 1);
        }

        this.el.innerHTML = '<span class="wrap">'+this.txt+'</span>';

        var that = this;
        var delta = 200 - Math.random() * 100;

        if (this.isDeleting) { delta /= 2; }

        if (!this.isDeleting && this.txt === fullTxt) {
        delta = this.period;
        this.isDeleting = true;
        } else if (this.isDeleting && this.txt === '') {
        this.isDeleting = false;
        this.loopNum++;
        delta = 500;
        }

        setTimeout(function() {
        that.tick();
        }, delta);
    };

    window.onload = function() {
        var elements = document.getElementsByClassName('typewrite');
        for (var i=0; i<elements.length; i++) {
            var toRotate = elements[i].getAttribute('data-type');
            var period = elements[i].getAttribute('data-period');
            if (toRotate) {
              new TxtType(elements[i], JSON.parse(toRotate), period);
            }
        }
        // INJECT CSS
        var css = document.createElement("style");
        css.type = "text/css";
        css.innerHTML = ".typewrite > .wrap { border-right: 0.08em solid #fff}";
        document.body.appendChild(css);

        const form = document.getElementById('form');

form.addEventListener('submit', function(event) {
  event.preventDefault();

  const formData = new FormData(form);
  formData.append('access_key', '810e3275-ca64-40d9-8244-0c70a99bf626');

  fetch('https://api.web3forms.com/submit', {
    method: 'POST',
    body: formData
  })
  .then(response => response.json())
  .then(data => {
    // handle successful form submission
  })
  .catch(error => {
    // handle error
  });

  // submit the form to the server
  form.submit();
});


    };
</script>
</body>

</html>